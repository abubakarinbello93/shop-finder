{
  "name": "my-ai-app",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp app.js dist/ || true",
    "start": "npx serve dist"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "serve": "^14.2.0"
  }
}
